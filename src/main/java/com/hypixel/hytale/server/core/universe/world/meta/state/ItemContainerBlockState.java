package com.hypixel.hytale.server.core.universe.world.meta.state;

/** Minimal marker stub for ItemContainerBlockState so plugin compiles when engine
 *  runtime types are not on the classpath. At runtime the engine's interface
 *  will be used instead.
 */
public interface ItemContainerBlockState {
    // marker
}
